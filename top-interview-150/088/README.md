# 88. Merge Sorted Array

## 問題概要

2つの昇順ソート済み整数配列 `nums1` と `nums2` が与えられます。これらをマージして1つの昇順ソート済み配列にしてください。

`nums1` の末尾には `nums2` を受け入れるための空きスペース（サイズ `n`）があらかじめ確保されており、全体のサイズは `m + n` となっています。追加の配列を作成せず、`nums1` を直接書き換えるインプレースアルゴリズムを実装する必要があります。

## 制約

- 時間計算量：O(m + n)
- 空間計算量：O(1)
