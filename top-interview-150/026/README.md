# 26. Remove Duplicates from Sorted Array

## 問題概要

昇順にソートされた整数配列 `nums` が与えられます。
各要素が**1回だけ**出現するように、インプレースで重複を削除してください。

要素の相対的な順序を保持したまま、ユニークな要素の数 `k` を返します。
配列の先頭 `k` 個にユニークな要素が並んでいれば、それ以降の内容は無視されます。

## 制約

- 追加の配列用メモリを確保してはいけない（空間計算量 $O(1)$）。
- 配列はあらかじめソートされている。
