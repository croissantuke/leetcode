# 27. Remove Element

## 問題概要

整数配列 `nums` と整数 `val` が与えられます。
`nums` から `val` と等しい要素をすべてインプレースで削除してください。
要素の並び順は変わっても構いません。

削除後、`val` ではない要素の数 `k` を返し、配列の先頭 `k` 個が `val` 以外の要素であるようにします。
配列の先頭 `k` 個以降の内容は無視されます。

## 制約

- 追加の配列用メモリを確保してはいけない（空間計算量 $O(1)$）。
